<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= typeof title !=='undefined' ? title : 'Micro Social' %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="/socket.io/socket.io.js"></script>

    <style>
        body {
            background: #f8f9fa;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        .main-content {
            flex: 1;
        }

        .modern-navbar {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            box-shadow: 0 4px 20px rgba(102, 126, 234, 0.3);
            padding: 15px 0;
        }

        .navbar-brand {
            font-weight: 800;
            font-size: 1.5rem;
            color: white !important;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
            transition: all 0.3s;
        }

        .navbar-brand:hover {
            transform: scale(1.05);
        }

        .nav-link {
            color: rgba(255, 255, 255, 0.85) !important;
            font-weight: 600;
            padding: 10px 20px !important;
            border-radius: 50px;
            transition: all 0.3s;
            margin: 0 5px;
        }

        .nav-link:hover {
            background: rgba(255, 255, 255, 0.2);
            color: white !important;
            transform: translateY(-2px);
        }

        .nav-link.active {
            background: rgba(255, 255, 255, 0.25);
            color: white !important;
        }

        .notification-btn {
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid rgba(255, 255, 255, 0.3);
            color: white;
            border-radius: 50px;
            padding: 10px 25px;
            font-weight: 600;
            transition: all 0.3s;
            position: relative;
        }

        .notification-btn:hover {
            background: rgba(255, 255, 255, 0.3);
            border-color: white;
            color: white;
            transform: scale(1.05);
        }

        .notification-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 100%);
            color: white;
            border-radius: 50%;
            padding: 4px 9px;
            font-size: 11px;
            font-weight: 700;
            box-shadow: 0 2px 10px rgba(255, 107, 107, 0.5);
            animation: pulse-badge 2s infinite;
        }

        @keyframes pulse-badge {

            0%,
            100% {
                transform: scale(1);
            }

            50% {
                transform: scale(1.1);
            }
        }

        .notification-dropdown {
            width: 350px;
            border-radius: 15px;
            border: none;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
            padding: 0;
            margin-top: 10px;
        }

        .dropdown-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            font-weight: 700;
            padding: 15px 20px;
            border-radius: 15px 15px 0 0;
        }

        .notification-item {
            padding: 15px 20px;
            border-bottom: 1px solid #f0f0f0;
            transition: all 0.2s;
            cursor: pointer;
        }

        .notification-item:hover {
            background: #f8f9fa;
        }

        .notification-item.unread {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
        }

        .logout-btn {
            background: linear-gradient(135deg, #ff6b6b 0%, #ee5a6f 100%);
            border: none;
            color: white;
            border-radius: 50px;
            padding: 10px 25px;
            font-weight: 600;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(255, 107, 107, 0.3);
        }

        .logout-btn:hover {
            transform: scale(1.05);
            box-shadow: 0 6px 20px rgba(255, 107, 107, 0.4);
            color: white;
        }
    </style>
</head>

<body>

    <% if (typeof okunmamisBildirimSayisi !=='undefined' && title !=='Kayıt Ol' && title !=='Giriş Yap' ) { %>
        <nav class="navbar navbar-expand-lg modern-navbar sticky-top">
            <div class="container-fluid container">
                <a class="navbar-brand" href="/panel">🚀 Micro Social</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item"><a class="nav-link <%= title === 'Ana Akış' ? 'active' : '' %>"
                                href="/panel">🏠 Akış</a></li>
                        <li class="nav-item"><a class="nav-link <%= title.includes('Profil') ? 'active' : '' %>"
                                href="/profil">👤 Profilim</a></li>
                        <li class="nav-item"><a class="nav-link <%= title.includes('Arkadaş') ? 'active' : '' %>"
                                href="/arkadaslarim">👥 Arkadaşlar</a></li>
                        <li class="nav-item"><a class="nav-link <%= title.includes('Sohbet') ? 'active' : '' %>"
                                href="/sohbet">💬 Sohbet</a></li>
                    </ul>
                    <div class="d-flex align-items-center gap-3">
                        <div class="dropdown">
                            <button class="notification-btn dropdown-toggle" type="button" data-bs-toggle="dropdown"
                                aria-expanded="false">
                                🔔 Bildirimler
                                <% if (okunmamisBildirimSayisi> 0) { %>
                                    <span class="notification-badge" id="bildirim-sayaci">
                                        <%= okunmamisBildirimSayisi %>
                                    </span>
                                    <% } %>
                            </button>
                            <ul class="dropdown-menu dropdown-menu-end notification-dropdown" id="bildirim-listesi">
                                <li class="dropdown-header">📬 Son Bildirimler</li>
                                <% if (typeof sonBildirimler !=='undefined' && sonBildirimler.length> 0) { %>
                                    <% sonBildirimler.forEach((bildirim)=> { %>
                                        <li>
                                            <a class="notification-item <%= !bildirim.okundu ? 'unread' : '' %>"
                                                href="/bildirim/oku?redirect=<%= encodeURIComponent(bildirim.link) %>"
                                                style="text-decoration: none; color: inherit; display: block;">
                                                <div style="font-size: 0.95rem;">
                                                    <%= bildirim.mesaj %>
                                                </div>
                                                <small style="color: #999; font-size: 0.85rem;">
                                                    <%= bildirim.zaman %>
                                                </small>
                                            </a>
                                        </li>
                                        <% }) %>
                                            <li class="dropdown-divider" style="margin: 0;"></li>
                                            <li
                                                style="text-align: center; padding: 12px; background: #f8f9fa; border-radius: 0 0 15px 15px;">
                                                <small style="color: #667eea; font-weight: 600;">Toplam <%= typeof
                                                        toplamBildirimSayisi !=='undefined' ? toplamBildirimSayisi : 0
                                                        %> Bildirim</small>
                                            </li>
                                            <% } else { %>
                                                <li class="notification-item" style="text-align: center; color: #999;">
                                                    <div style="padding: 20px;">
                                                        <div style="font-size: 40px; margin-bottom: 10px;">📭</div>
                                                        Hiç bildirim yok
                                                    </div>
                                                </li>
                                                <% } %>
                            </ul>
                        </div>
                        <a class="logout-btn" href="/cikis">⛔ Çıkış</a>
                    </div>
                </div>
            </div>
        </nav>
        <% } %>

            <div class="main-content">
                <div class="container mt-4 mb-5">